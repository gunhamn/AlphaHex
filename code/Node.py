class node:
    def __init__(self) -> None:
        pass
        #children = np.array(node, node, node) where length equals possible moves to make
        #state
        #value

    #rollout
    #for i in range(len(children)):
        #children[i]=node(state where action has been taken)
        #children[i].rollout()?
    